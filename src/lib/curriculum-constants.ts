// src/lib/curriculum-constants.ts
// Single home for cross-cutting curriculum defaults. These are FALLBACKS —
// the real per-level values live on the Level row (level.sheetsPerDay etc.);
// use these only when no level is loaded.
export const DEFAULT_SHEETS_PER_DAY = 3;
